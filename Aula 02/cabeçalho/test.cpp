#include <iostream>
#include "test.h"

int imprimir(int x) {
	std::cout << x << std::endl;
}

int somar(int x, int y) {
	return (x + y);
}

int subtrair(int x, int y) {
	return (x - y);
}

int mutiplicar(int x, int y) {
	return (x * y);
}

int dividir(int x, int y) {
	return (x/y);
}

